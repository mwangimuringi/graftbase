import React from "react";

const AuthProviders = () => {
  return (
    <div className="auth-providers">
      This is the AuthProviders component. It will be used to display the
      different auth providers.
      <h3>AuthProviders</h3>
      <p>
        This is the AuthProviders component. It will be used to display the
        different auth providers.
      </p>
    </div>
  );
};

export default AuthProviders;
